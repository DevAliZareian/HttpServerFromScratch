namespace HttpServer.Protocol
{
    public enum HttpMethodType
    {
        GET,
        POST,
        PUT,
        DELETE,
        HEAD,
        OPTIONS,
        PATCH,
        TRACE,
        CONNECT
    }
}